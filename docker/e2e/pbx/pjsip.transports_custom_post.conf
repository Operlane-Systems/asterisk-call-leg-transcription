; The lab SIP client is another container on this Docker bridge.  Treat that
; subnet as local so PJSIP advertises the PBX container address in SDP rather
; than Docker Desktop's host-facing public address.
[0.0.0.0-udp](+)
local_net=172.30.0.0/24
